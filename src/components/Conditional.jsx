export default function Conditional(){
	const cond = true;
	if(cond) {
		return ( <div>
			<h2>This is a conditional statement</h2>
			</div>
		)
		} else {
		return ( <div>
			<h2>Your code works</h2>
			</div>
		)
		}
}
